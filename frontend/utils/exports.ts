// Common exports for easier imports across the app
export {
    AppImages, BouquetCategories,
    BouquetImages,
    getBouquetImage
} from '../assets';
export { Colors, getColorByCategory } from '../constants/Colors';

// Screen exports
export {
    CartScreen, CatalogScreen, CheckoutScreen, FavoriteScreen, HomeScreen, LoginScreen, ProfileScreen, SplashScreen
} from '../screens';

// Type exports (if any)
// export type { BouquetCategory, BouquetItem } from '../types';
